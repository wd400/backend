syntax = "proto3";

message FeedRequest {
    string access_token = 1;

    enum FeedType {
        LAST_DAY = 0;
        LAST_WEEK = 1;
        LAST_MONTH = 2;
        ALL_TIME = 3;
        EMERGENCY = 4;
        LAST_ACTIVITY = 5;
      }

    FeedType feed_type = 2;

    int32 offset = 3;
}

